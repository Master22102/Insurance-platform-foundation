/**
 * F-6.5.14 — server-side PDF generation entrypoint (@react-pdf/renderer).
 * (Spec file name `generate-pdf.ts`; implementation lives in `render-claim-packet-pdf.ts`.)
 */
export { renderClaimPacketPdfBuffer } from './render-claim-packet-pdf';
